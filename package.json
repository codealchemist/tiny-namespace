{
  "name": "tiny-namespace",
  "version": "1.0.0",
  "description": "Set and get data on a namespace singleton without using global scope at all.",
  "main": "index.js",
  "scripts": {
    "test": "standard && mocha --watch --recursive --reporter spec",
    "test-focus": "standard-focus"
  },
  "keywords": [
    "namespace"
  ],
  "author": "Alberto Miranda",
  "repository": "codealchemist/tiny-namespace",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "standard-focus": "^1.1.2"
  },
  "standard": {
    "globals": [
      "describe",
      "it",
      "beforeEach"
    ]
  }
}
